<%= form_for(@admin_agency) do |f| %>
  <% if @admin_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_agency.errors.count, "error") %> prohibited this admin_agency from being saved:</h2>

      <ul>
      <% @admin_agency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :domain %><br />
    <%= f.text_field :domain %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
